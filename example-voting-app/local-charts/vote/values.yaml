# Default values for vote.
# This is a YAML-formatted file.
# Declare name/value pairs to be passed into your templates.
# name: value
image: 
  repository: docker.io/jamesoverdocker/example-voting-app-vote
  tag: latest

redis:
  usePassword: false

replicaCount: 1
